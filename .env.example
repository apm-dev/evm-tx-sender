# Copy to .env and fill in real values. Never commit .env to git.

# Signer private keys (at least one required)
# Format: TX_SENDER_KEYS_<0xADDRESS>=<hex-private-key>
TX_SENDER_KEYS_0xYourAddress=your_private_key_hex_without_0x_prefix
